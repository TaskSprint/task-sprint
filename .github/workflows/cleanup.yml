name: Cleanup Actions

on:
  pull_request:
  pull_request_review:
    types: [ submitted, edited ]

jobs:
  prettier:
    if: ${{ github.event.review.state == 'APPROVED' }}
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Run Prettier check
        id: prettier_check
        run: npm run prettier:check
        continue-on-error: true

      - name: Add auto-prettify label
        id: add_auto_prettify_label
        if: ${{ steps.prettier_check.outcome == 'failure' }}
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ["🖌️ auto prettify"]
            })

      - name: Prettify
        id: prettify
        if: ${{ steps.add_auto_prettify_label.outcome == 'success' }}
        run: npm run prettier:format

      - name: Commit changes by actor
        id: commit_changes_by_actor
        if: ${{ steps.prettify.outcome == 'success' }}
        uses: stefanzweifel/git-auto-commit-action@v5
        continue-on-error: true
        with:
          commit_message: "[BOT] 🖌️ Auto prettify"
          push_options: ""
          skip_dirty_check: false

      - name: Commit changes by event issue user
        if: ${{ steps.prettify.outcome == 'success' && steps.commit_changes_by_actor.outcome == 'failure' }}
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "[BOT] 🖌️ Auto prettify"
          push_options: ""
          skip_dirty_check: false
          commit_author: "${{ github.event.issue.user.login }} <${{ github.event.issue.user.login.id }}+${{ github.event.issue.user.login }}@users.noreply.github.com>"
